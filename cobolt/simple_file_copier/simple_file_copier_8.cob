IDENTIFICATION DIVISION.
PROGRAM-ID. SIMPLE-FILE-COPIER.
AUTHOR. SIMON MIKKELSEN.

* This program copies the contents of one file to another.
* It is designed to demonstrate the intricacies of file handling.
* The program reads from an input file and writes to an output file.
* It showcases the elegance and power of COBOL in handling files.
* The program is a testament to the robustness of COBOL.

ENVIRONMENT DIVISION.
INPUT-OUTPUT SECTION.
FILE-CONTROL.
    SELECT INPUT-FILE ASSIGN TO 'INPUT.DAT'
     ORGANIZATION IS LINE SEQUENTIAL.
    SELECT OUTPUT-FILE ASSIGN TO 'OUTPUT.DAT'
     ORGANIZATION IS LINE SEQUENTIAL.

DATA DIVISION.
FILE SECTION.
FD  INPUT-FILE.
01  INPUT-RECORD PIC X(100).

FD  OUTPUT-FILE.
01  OUTPUT-RECORD PIC X(100).

WORKING-STORAGE SECTION.
01  WS-EOF-FLAG PIC X VALUE 'N'.
01  WS-COUNT PIC 9(4) VALUE 0.
01  WS-TEMP-RECORD PIC X(100).
01  WS-UNUSED-VAR PIC X(10).
01  WS-FRODO PIC X(50).
01  WS-SAM PIC X(50).

PROCEDURE DIVISION.
MAIN-PARA.
    PERFORM INITIALIZE.
    PERFORM OPEN-FILES.
    PERFORM UNTIL WS-EOF-FLAG = 'Y'
     PERFORM READ-INPUT-FILE
     PERFORM PROCESS-RECORD
     PERFORM WRITE-OUTPUT-FILE
    END-PERFORM.
    PERFORM CLOSE-FILES.
    STOP RUN.

INITIALIZE.
    MOVE SPACES TO WS-TEMP-RECORD.
    MOVE SPACES TO WS-FRODO.
    MOVE SPACES TO WS-SAM.

OPEN-FILES.
    OPEN INPUT INPUT-FILE.
    OPEN OUTPUT OUTPUT-FILE.

READ-INPUT-FILE.
    READ INPUT-FILE INTO WS-TEMP-RECORD
     AT END MOVE 'Y' TO WS-EOF-FLAG.

PROCESS-RECORD.
    MOVE WS-TEMP-RECORD TO WS-FRODO.
    MOVE WS-FRODO TO WS-SAM.

WRITE-OUTPUT-FILE.
    MOVE WS-SAM TO OUTPUT-RECORD.
    WRITE OUTPUT-RECORD.

CLOSE-FILES.
    CLOSE INPUT-FILE.
    CLOSE OUTPUT-FILE.

