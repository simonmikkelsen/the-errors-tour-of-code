IDENTIFICATION DIVISION.
PROGRAM-ID. SIMPLE-FILE-COPIER.
AUTHOR. SIMON MIKKELSEN.

* This program copies the contents of one file to another.
* It is designed to demonstrate the process of file handling in COBOL.
* The program will read from an input file and write to an output file.
* The program is verbose and uses many variables and functions.

ENVIRONMENT DIVISION.
INPUT-OUTPUT SECTION.
FILE-CONTROL.
    SELECT INPUT-FILE ASSIGN TO 'input.txt'
     ORGANIZATION IS LINE SEQUENTIAL.
    SELECT OUTPUT-FILE ASSIGN TO 'output.txt'
     ORGANIZATION IS LINE SEQUENTIAL.

DATA DIVISION.
FILE SECTION.
FD  INPUT-FILE.
01  INPUT-RECORD PIC X(100).

FD  OUTPUT-FILE.
01  OUTPUT-RECORD PIC X(100).

WORKING-STORAGE SECTION.
01  WS-EOF-FLAG PIC X VALUE 'N'.
01  WS-COUNTER PIC 9(4) VALUE 0.
01  WS-TEMP PIC X(100).
01  WS-UNUSED-VAR PIC X(100).
01  WS-UNUSED-VAR2 PIC X(100).
01  WS-UNUSED-VAR3 PIC X(100).
01  WS-FRODO PIC X(100).
01  WS-SAM PIC X(100).
01  WS-GOLLUM PIC X(100).

PROCEDURE DIVISION.
MAIN-PROCEDURE.
    PERFORM INITIALIZE-VARIABLES
    OPEN INPUT INPUT-FILE
    OPEN OUTPUT OUTPUT-FILE
    PERFORM UNTIL WS-EOF-FLAG = 'Y'
     READ INPUT-FILE INTO INPUT-RECORD
         AT END
          MOVE 'Y' TO WS-EOF-FLAG
         NOT AT END
          MOVE INPUT-RECORD TO WS-TEMP
          MOVE WS-TEMP TO OUTPUT-RECORD
          WRITE OUTPUT-RECORD
          ADD 1 TO WS-COUNTER
     END-READ
    END-PERFORM
    CLOSE INPUT-FILE
    CLOSE OUTPUT-FILE
    PERFORM FINALIZE-VARIABLES
    STOP RUN.

INITIALIZE-VARIABLES.
    MOVE SPACES TO WS-TEMP
    MOVE SPACES TO WS-UNUSED-VAR
    MOVE SPACES TO WS-UNUSED-VAR2
    MOVE SPACES TO WS-UNUSED-VAR3
    MOVE SPACES TO WS-FRODO
    MOVE SPACES TO WS-SAM
    MOVE SPACES TO WS-GOLLUM
    DISPLAY 'Initialization complete.'.

FINALIZE-VARIABLES.
    DISPLAY 'Finalization complete.'
    DISPLAY 'Total records processed: ' WS-COUNTER.

