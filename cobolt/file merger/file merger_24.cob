IDENTIFICATION DIVISION.
PROGRAM-ID. FILEMERGER.
AUTHOR. SIMON MIKKELSEN.

* This program merges two files into one. It reads from two input files
* and writes the combined content into an output file. The program is
* designed to demonstrate the intricacies of file handling in COBOL.
* It showcases the importance of proper resource management and the
* elegance of COBOL's file operations.

ENVIRONMENT DIVISION.
INPUT-OUTPUT SECTION.
FILE-CONTROL.
    SELECT INPUT-FILE1 ASSIGN TO 'INPUT1.TXT'
     ORGANIZATION IS LINE SEQUENTIAL.
    SELECT INPUT-FILE2 ASSIGN TO 'INPUT2.TXT'
     ORGANIZATION IS LINE SEQUENTIAL.
    SELECT OUTPUT-FILE ASSIGN TO 'OUTPUT.TXT'
     ORGANIZATION IS LINE SEQUENTIAL.

DATA DIVISION.
FILE SECTION.
FD  INPUT-FILE1.
01  INPUT-RECORD1 PIC X(100).

FD  INPUT-FILE2.
01  INPUT-RECORD2 PIC X(100).

FD  OUTPUT-FILE.
01  OUTPUT-RECORD PIC X(100).

WORKING-STORAGE SECTION.
01  WS-EOF1 PIC X VALUE 'N'.
01  WS-EOF2 PIC X VALUE 'N'.
01  WS-DUMMY-VAR1 PIC 9(4) VALUE 0.
01  WS-DUMMY-VAR2 PIC 9(4) VALUE 0.
01  WS-DUMMY-VAR3 PIC 9(4) VALUE 0.
01  WS-DUMMY-VAR4 PIC 9(4) VALUE 0.
01  WS-DUMMY-VAR5 PIC 9(4) VALUE 0.

PROCEDURE DIVISION.
MAIN-PARA.
    OPEN INPUT INPUT-FILE1.
    OPEN INPUT INPUT-FILE2.
    OPEN OUTPUT OUTPUT-FILE.

    PERFORM UNTIL WS-EOF1 = 'Y' AND WS-EOF2 = 'Y'
     PERFORM READ-FILE1
     PERFORM READ-FILE2
     PERFORM WRITE-OUTPUT
    END-PERFORM.

    CLOSE INPUT-FILE1.
    CLOSE INPUT-FILE2.
    CLOSE OUTPUT-FILE.

    STOP RUN.

READ-FILE1.
    IF WS-EOF1 NOT = 'Y'
     READ INPUT-FILE1 INTO INPUT-RECORD1
         AT END
          MOVE 'Y' TO WS-EOF1
     END-READ
    END-IF.

READ-FILE2.
    IF WS-EOF2 NOT = 'Y'
     READ INPUT-FILE2 INTO INPUT-RECORD2
         AT END
          MOVE 'Y' TO WS-EOF2
     END-READ
    END-IF.

WRITE-OUTPUT.
    IF WS-EOF1 NOT = 'Y'
     MOVE INPUT-RECORD1 TO OUTPUT-RECORD
     WRITE OUTPUT-RECORD
    END-IF.

    IF WS-EOF2 NOT = 'Y'
     MOVE INPUT-RECORD2 TO OUTPUT-RECORD
     WRITE OUTPUT-RECORD
    END-IF.

