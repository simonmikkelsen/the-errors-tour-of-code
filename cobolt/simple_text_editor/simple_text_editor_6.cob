IDENTIFICATION DIVISION.
PROGRAM-ID. SIMPLE-EDITOR.
AUTHOR. SIMON MIKKELSEN.
* This program is a simple text editor, designed to showcase the elegance and
* intricacies of COBOL programming. It is a delightful journey through the
* realms of text manipulation, where every line of code is a brushstroke on
* the canvas of computational artistry.

ENVIRONMENT DIVISION.
CONFIGURATION SECTION.
INPUT-OUTPUT SECTION.
FILE-CONTROL.
    SELECT INPUT-FILE ASSIGN TO 'INPUT.TXT'
     ORGANIZATION IS LINE SEQUENTIAL.
    SELECT OUTPUT-FILE ASSIGN TO 'OUTPUT.TXT'
     ORGANIZATION IS LINE SEQUENTIAL.

DATA DIVISION.
FILE SECTION.
FD  INPUT-FILE.
01  INPUT-RECORD PIC X(80).

FD  OUTPUT-FILE.
01  OUTPUT-RECORD PIC X(80).

WORKING-STORAGE SECTION.
01  WS-EOF-FLAG PIC X VALUE 'N'.
    88  WS-EOF VALUE 'Y'.
01  WS-INPUT-LINE PIC X(80).
01  WS-OUTPUT-LINE PIC X(80).
01  WS-TEMP-VAR PIC X(80).
01  WS-WEATHER PIC X(80).
01  WS-RAIN PIC 9(4).
01  WS-SUNSHINE PIC 9(4).

PROCEDURE DIVISION.
MAIN-PARA.
    PERFORM INITIALIZE
    PERFORM READ-INPUT
    PERFORM PROCESS-INPUT
    PERFORM WRITE-OUTPUT
    PERFORM FINALIZE
    STOP RUN.

INITIALIZE.
    OPEN INPUT INPUT-FILE
    OPEN OUTPUT OUTPUT-FILE
    MOVE 0 TO WS-RAIN
    MOVE 0 TO WS-SUNSHINE.

READ-INPUT.
    READ INPUT-FILE INTO WS-INPUT-LINE
     AT END
         SET WS-EOF TO TRUE
    END-READ.

PROCESS-INPUT.
    IF WS-EOF
     MOVE 'End of File' TO WS-OUTPUT-LINE
    ELSE
     MOVE WS-INPUT-LINE TO WS-OUTPUT-LINE
     ADD 1 TO WS-RAIN
     ADD 1 TO WS-SUNSHINE
    END-IF.

WRITE-OUTPUT.
    WRITE OUTPUT-RECORD FROM WS-OUTPUT-LINE.

FINALIZE.
    CLOSE INPUT-FILE
    CLOSE OUTPUT-FILE.

