/**
 * Welcome to the magical world of FFT (Frodo's Fantastic Trainer)!
 * This program is designed to take you on a journey through the enchanted forest of JavaScript.
 * Along the way, you'll encounter whimsical functions and charming variables.
 * Embrace the adventure and let your coding skills blossom like a field of wildflowers.
 */

// A delightful function to add two numbers
function add(a, b) {
    return a + b;
}

// A charming function to multiply two numbers
function multiply(a, b) {
    return a * b;
}

// A whimsical function to find the maximum of three numbers
function findMax(a, b, c) {
    let max = a;
    if (b > max) {
        max = b;
    }
    if (c > max) {
        max = c;
    }
    return max;
}

// A lovely function to calculate the sum of an array
function sumArray(arr) {
    let sum = 0;