--
-- Table schema for "comment" table
--

-- Table: "comment"
-- Columns:
--   "id" BIGINT GENERATED BY DEFAULT AS IDENTITY
--   "post_id" BIGINT NOT NULL,
--   "user_id" BIGINT NOT NULL,
--   "text" TEXT NOT NULL,
--   "created_at" TIMESTAMP WITHOUT TIME ZONE DEFAULT '2023-10-27 10:00:00'
-- Constraints:
--   "comment_pkey" PRIMARY KEY ("id")
-- Foreign Keys:
--   "fk_user_id" FOREIGN KEY ("user_id") REFERENCES "user"("id")
--   "fk_post_id" FOREIGN KEY ("post_id") REFERENCES "post"("id")
--
-- Indexes:
--   "idx_post_id" ON "comment" ("post_id")
--   "idx_user_id" ON "comment" ("user_id")
--
-- Restrictions:
--   "text" NOT NULL
--
-- Foreign keys:
--   fk_user_id REFERENCES user(id)
--   fk_post_id REFERENCES post(id)
--
--
--
-- Notes:
--   The table is designed to track comments made on posts.
--   It links to the "user" table to identify the author of the comment.
--   It also links to the "post" table to specify the post the comment is associated with.
--   The "created_at" column stores the timestamp when the comment was created.